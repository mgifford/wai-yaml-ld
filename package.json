{
  "name": "wai-yaml-ld",
  "version": "1.0.0",
  "private": true,
  "description": "Static GraphQL-style API builder for WAI YAML-LD datasets",
  "type": "module",
  "scripts": {
    "build:api": "node build-api.js",
    "setup": "npm install && npm run build:api",
    "mcp:bridge": "node mcp-bridge.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "graphql": "^16.11.0",
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}